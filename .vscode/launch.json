{
  "version": "0.2.0",
  "configurations": [
    {
      //  "name": "(lldb) Launch",
      //  "type": "lldb", for LLDB
      //  "type": "cppdbg", for GDB
      "name": "Launch2",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/out/task.out",
      "args": [],
      // 以文件所在的路径为根路径
      "cwd": "${workspaceFolder}"
      // "${fileDirname}"

    }
  ]
}